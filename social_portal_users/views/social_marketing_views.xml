<odoo>
    <data> <!-- Encapsulates all view definitions -->
        <record id="view_social_account_tree" model="ir.ui.view">
            <field name="name">social.stream.post.tree</field>
            <field name="model">social.stream.post</field>
            <field name="arch" type="xml">
                <tree>
                    <!-- Uncomment these fields if needed -->
<!--                     <field name="name"/>-->
<!--                     <field name="social_network"/>-->
                    <field name="user_id" invisible="1"/>
                </tree>
            </field>
<!--                        <field name="domain">[('user_id', '=', uid)]</field> &lt;!&ndash; Filter by the current user &ndash;&gt;-->
        </record>
    </data>
</odoo>
